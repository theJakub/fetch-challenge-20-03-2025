import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#7575D1',
      light: '#9E9EE0',
      dark: '#5252B5',
      contrastText: '#FFFFFF',
    },
  },
});

export default theme;
